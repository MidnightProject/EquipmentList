<UserControl x:Class="EquipmentList.View.Building"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EquipmentList.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid ItemsSource="{Binding BuildingTable}" AlternatingRowBackground="{DynamicResource {x:Static SystemColors.GradientInactiveCaptionBrushKey}}" IsReadOnly="True" SelectionMode="Single" AutoGenerateColumns="False" SelectedIndex="{Binding SelectedIndex}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding NAME}" />
                <DataGridTextColumn Header="Country" Binding="{Binding COUNTRY}" />
                <DataGridTextColumn Header="City" Binding="{Binding CITY}" />
                <DataGridTextColumn Header="Address" Binding="{Binding ADDRESS}" />
                <DataGridTextColumn Header="Postcode" Binding="{Binding POSTCODE}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
